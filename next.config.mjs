/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // i18n configuration is removed as we're using App Router
  // Internationalization is now handled through route groups and middleware
  
  // Add other Next.js configurations here if needed
};

export default nextConfig;
